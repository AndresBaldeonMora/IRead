import React from 'react';
import { View } from 'react-native';

// Placeholder route — the tab button opens the AddBookModal via custom tabBarButton in _layout.
export default function AgregarPlaceholder() {
  return <View />;
}
